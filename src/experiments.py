from options import ExperimentOptions

# experiments = [
#     ExperimentOptions(experiment='both', game_size=30, max_len=3, vocab_size=60),
# ]

experiments = [
    # ExperimentOptions(experiment='both', game_size=2, max_len=4, vocab_size=7),
    ExperimentOptions(experiment='both', game_size=5, max_len=4, vocab_size=7),
]